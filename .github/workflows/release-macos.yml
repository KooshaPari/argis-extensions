name: Release macOS (argis-extensions)

# Calls the reusable workflow at kooshapari/phenotype-fleet-ops.
# Self-hosted runners only (zero billed minutes).
# All Apple Developer secrets live in Infisical project 8efe392e-56a6-4c3c-89f9-8141183dd7e8.

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

concurrency:
  group: macos-release-argis-extensions-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  macos:
    name: Build, sign, notarize argis-extensions
    uses: kooshapari/phenotype-fleet-ops/.github/workflows/macos-release.yml@main
    with:
      app-name: argis-extensions
      app-bundle-id: app.phenotype.argis-extensions
      build-cmd: |
          pnpm install --frozen-lockfile
          pnpm --filter extensions tauri build --target universal-apple-darwin
          cp -R apps/extensions/src-tauri/target/universal-apple-darwin/release/bundle/macos/argis-extensions.app dist/macos/
      entitlements-file: apps/extensions/src-tauri/Entitlements.plist
      artifact-dir: dist/macos
      create-dmg: true
      sign-identity: "Developer ID Application"
    secrets:
      INFISICAL_TOKEN: ${{ secrets.INFISICAL_TOKEN }}
