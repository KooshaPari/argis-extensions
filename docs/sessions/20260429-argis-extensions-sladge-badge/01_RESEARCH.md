# Research

## Repo Evidence

The README describes Bifrost Extensions as a clean extension layer for the
Bifrost LLM gateway. The docs include an AI/ML architecture page covering
intelligent routing, model profiles, local model management, RouteLLM, and
prompt transformation between models.

## Local State

The canonical `argis-extensions` checkout had unrelated local ADR, test coverage,
and progress document changes before this pass. The badge was prepared in an
isolated worktree.

## Decision

Add the sladge badge as governance metadata and defer canonical integration
until the unrelated local changes are handled.
